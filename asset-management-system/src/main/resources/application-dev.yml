server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://gdnt_ams_mysql_db:3306/gdnt_dev
    # url: jdbc:mysql://localhost:3306/gdnt_dev
    username: root
    password: AA@@11bb22cc33
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format: true
    database: mysql
    database-platform: MySQLDialect
  
# Take the span id and trace id from the Mapped Diagnostic Context (MDC) and
# put them into the log message.
logging:
  file:
    path: logs/${spring.application.name}
    name: ${logging.file.path}.log
  pattern:
    console: " %d{yyyy-MM-dd HH:mm:ss} %-5level [${spring.application.name:},%X{traceId:-},%X{spanId:-}] %-32.32logger{36} - %msg%n"
    file: " %d{yyyy-MM-dd HH:mm:ss}] %-5level [${spring.application.name:},%X{traceId:-},%X{spanId:-}] %-32.32logger{36} - %msg%n"
  logback:
    rollingpolicy:
      max-file-size: 100KB
      file-name-pattern: logs/${spring.application.name}.%d{yyyyMMdd}.%i.log


  